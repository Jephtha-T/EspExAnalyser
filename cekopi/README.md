# Cekopi

Flutter app for recording espresso videos, sending them to the analyser backend, and saving the results in a small coffee library.

## Run

```bash
flutter pub get
flutter run
```

The backend URL is set in `lib/core/config/backend_config.dart`.
